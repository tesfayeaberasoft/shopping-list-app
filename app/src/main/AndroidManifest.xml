<?xml version="1.0" encoding="utf-8"?>
<!-- Shopping List App - Android Manifest Configuration
     Defines app permissions, activities, services, and broadcast receivers
     
     Key Components:
     - Activities: MainActivity, AuthActivity, SettingsActivity, etc.
     - Services: BackgroundSyncService, ReminderBroadcastReceiver
     - Permissions: Camera, Audio, Storage, Notifications
     - Providers: FileProvider for file sharing
     
     Permissions:
     - INTERNET: Cloud sync and Firebase
     - CAMERA: Barcode scanning and photo capture
     - RECORD_AUDIO: Voice input
     - READ/WRITE_EXTERNAL_STORAGE: File access
     - POST_NOTIFICATIONS: Push notifications
     
     API Levels:
     - Min SDK: 21 (Android 5.0)
     - Target SDK: 34 (Android 14)
     
     Security Features:
     - Network security configuration
     - Certificate pinning for Firebase
     - Secure data storage
     - Permission runtime checks
     
     Background Services:
     - Sync service for cloud data
     - Reminder notifications
     - Periodic backup
     - Data synchronization
     
     Intent Filters:
     - Main launcher activity
     - Share intent handling
     - Deep linking support
     - Barcode scanning intents
     
     Features:
     - Camera feature (optional)
     - Microphone feature (optional)
     - Touchscreen support
     - Multi-window support
     
     Broadcast Receivers:
     - Boot completion receiver
     - Network state receiver
     - Reminder notification receiver
     - Sync completion receiver
     
     Content Providers:
     - FileProvider for secure file sharing
     - Data sharing between components
     - External app integration
     
     Metadata:
     - Firebase configuration
     - Analytics settings
     - Crash reporting
     - Performance monitoring
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".ShoppingListApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingListApp">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ShoppingListApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.WelcomeActivity"
            android:theme="@style/Theme.ShoppingListApp.NoActionBar" />

        <activity android:name=".activities.AuthActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.MainActivity"
            android:launchMode="singleTop" />
        <activity android:name=".activities.ListDetailActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.AnalyticsActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity android:name=".activities.PantryActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity android:name=".activities.RemindersActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity android:name=".activities.ShoppingHistoryActivity"
            android:parentActivityName=".activities.MainActivity" />

        <receiver android:name=".services.ReminderBroadcastReceiver"
            android:enabled="true"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
